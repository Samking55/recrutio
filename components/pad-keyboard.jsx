import { View, Text, TouchableOpacity } from "react-native"

function PadKeyboard({setValue}) {
    // use set value to alter the state of the keyboard output
  return (
    <View>

    </View>
  )
}

export default PadKeyboard